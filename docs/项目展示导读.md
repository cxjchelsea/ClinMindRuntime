# ClinMindRuntime 项目展示导读

> 本文档用于快速说明 ClinMindRuntime 的项目定位、当前实现状态、核心架构、验证方式和展示重点。  
> 它是项目展示入口，不替代完整系统设计、技术实现总方案和各 Phase 详细设计。

---

# 一、项目一句话介绍

ClinMindRuntime 是一个面向医疗问诊场景的受控 AI Runtime 原型。

它的重点不是让大模型直接生成最终回答，而是让 AI 能力在一个可控、可评估、可追踪、可回滚、可治理的 Runtime 中运行。

```text
普通 RAG / Agent：输入 → 检索 / 工具 → LLM → 输出
ClinMindRuntime：输入 → RuntimeState → SafetyGate → EvidenceGraph → DecisionBoundary → 分角色输出 → RuntimeTrace → Evaluation → Candidate → Review → Persistence / Audit → Console Governance
```

---

# 二、项目要解决的问题

在高风险问诊场景中，AI 系统不能只追求回答自然，还需要处理：

```text
1. 高风险信号不能被忽略。
2. 患者端不能看到内部推理和医生端信息。
3. 知识、规则、经验、模型需要有版本和来源。
4. 每次输出需要能追踪依据。
5. 能力需要通过病例集和指标评估。
6. 外部模型、RAG、MCP 等能力不能绕过 Runtime 主控。
7. 运行结果即使有价值，也只能先变成候选，不能自动生效。
8. 核心治理对象需要持久化、审计和恢复能力。
9. 已持久化对象需要通过最小 Console API 被安全查询和权限控制。
```

---

# 三、当前实现状态

当前仓库已经完成：

```text
Phase 1-P0：Runtime MVP
Phase 2-P0：共享能力资产原型
Phase 3-P0：训练与评估闭环 MVP，并已冻结
Phase 4-P0：经验候选与训练数据候选沉淀机制，并已冻结
Phase 4-P1：候选脱敏、SourceRef 强校验、Review 记录，并已冻结
Phase 5-P0：持久化与治理底座，并已冻结（commit 2ad656d）
Phase 5-P1：最小 Console 与访问治理详细设计
```

当前阶段：

```text
Phase 5-P1 implementation ready
下一步：Phase5-P1-A ActorContext 与 RBAC-lite 基础
```

当前暂不实现：

```text
真实 RAG / GraphRAG
Python AI Provider
完整前端 Console
模型训练 / 后训练
MCP / LangGraph / Agent SDK
正式登录 / OAuth / 多租户
正式医生审核流
正式经验库上线
TrainingDatasetVersion 发布
```

---

# 四、核心架构链路

## 4.1 Runtime 主链路

```text
User Input
→ RuntimeService
→ EntryAssessment
→ CaseFrame
→ KnowledgeContext
→ ExperienceContext
→ SafetyGate
→ DifferentialDiagnosisBoard
→ EvidenceGraph
→ QuestionTestPolicy
→ DecisionBoundary
→ PatientOutput / ClinicianReport
→ RuntimeTrace
```

## 4.2 Asset Provider 链路

```text
Runtime Core
→ Provider Interface
→ YAML Asset Package
→ versioned asset refs
→ RuntimeTrace assets_used
```

## 4.3 Evaluation 闭环

```text
EvaluationCaseSet
→ RuntimeEvaluationRunner
→ RuntimeService.startRuntime / continueRuntime
→ RuntimeState / RuntimeTrace / API Response
→ EvaluationScorer
→ EvaluationItemResult
→ EvaluationResult
→ CapabilityProfileUpdateProposal
```

## 4.4 Candidate 沉淀与治理链路

```text
EvaluationRun
→ EvaluationResult
→ EvaluationItemResult
→ RegressionFinding / SafetyViolation / RuntimeCaseExecution
→ CandidateGenerationService
→ CandidateSanitizer
→ CandidateSourceRefFactory / Validator
→ ExperienceCandidate / TrainingExampleCandidate
→ CandidateReviewService
→ CandidateReviewRecord
→ Review Required / Approved / Rejected / Deprecated
```

## 4.5 Persistence 治理链路

```text
Runtime / Evaluation / Candidate / Review Service
→ Store Interface
→ InMemory implementation
→ PostgreSQL implementation
→ AuditLogService
→ Persistence health / Audit query
```

## 4.6 Phase 5-P1 Console Governance 链路

```text
Console / Debug Request
→ DebugTokenFilter
→ ActorContextResolver
→ AccessPolicy / RolePolicy
→ ConsoleQueryService
→ SafeConsoleDtoMapper
→ Store / Repository
→ AuditLogService
→ Console API Response
```

---

# 五、当前最值得展示的能力

## 5.1 受控 Runtime，而不是普通问答链

一次交互会进入 Runtime 实例，并维护状态、证据、安全门、输出边界和 Trace。

## 5.2 患者端 / 医生端输出隔离

患者端只返回安全表达，医生端可以看到更多内部辅助信息。

## 5.3 Asset Package 版本治理

系统会绑定 asset_package_id 和 asset_package_version，版本不匹配时进入安全处理，而不是静默继续。

## 5.4 EvaluationScorer 策略体系

当前已有评分维度：

```text
EntryAssessmentScorer
SafetyGateScorer
PatientBoundaryScorer
DdxCoverageScorer
NextActionScorer
TraceCompletenessScorer
AssetVersionTraceScorer
```

## 5.5 Candidate 候选沉淀与 Review 边界

系统可以把失败、风险和回归结果转化为：

```text
ExperienceCandidate
TrainingExampleCandidate
```

Candidate 经过脱敏和 SourceRef 强校验，并可记录 review 决策；但即使 review_status 变成 `APPROVED`，也不会自动上线经验、不会自动进入训练集、不会自动修改资产包或 CapabilityProfile。

## 5.6 PostgreSQL-backed governance runtime

Phase 5-P0 已经把核心治理对象推进到 PostgreSQL 持久化、Repository 双实现和 AuditLog 最小治理。

## 5.7 最小 Console 与访问治理方向

Phase 5-P1 设计已经明确：下一步不是直接做完整前端，而是先做 ActorContext、RBAC-lite、Safe Console DTO、Audit Center 和 Console API。

---

# 六、5 分钟 Demo 路径

### 1. 启动

```powershell
set JAVA_HOME=D:\cxj\software\jdk21
mvn -DskipTests package
java -jar target\clinmind-runtime-0.1.0-SNAPSHOT.jar
```

### 2. 跑一次 Runtime（高风险胸痛）

`POST /api/v1/runtime/start` — 输入「胸口闷，活动后更明显，出汗」

期望：`safety_gate_triggered`；患者输出含「风险信号」；响应中 `differential_board` 为 `null`。

### 3. 跑一次 Evaluation

`POST /api/v1/debug/evaluations/runs`

```json
{
  "case_set_id": "phase3-default",
  "case_set_version": "0.3.0",
  "asset_package_id": "phase2-default",
  "asset_package_version": "0.2.0",
  "include_tags": ["high_risk"],
  "fail_fast": false
}
```

### 4. 生成 Candidate 并 Review

```text
POST /api/v1/debug/candidates/generations/from-evaluation/{run_id}
GET /api/v1/debug/candidates/generations/{generation_id}
POST /api/v1/debug/candidates/training-example-candidates/{candidate_id}/review
GET /api/v1/debug/candidates/{candidate_id}/reviews
```

### 5. Phase 5-P1 当前状态

Phase 5-P1 目前完成的是详细设计，不是代码实现。下一步应先实现 ActorContext 与 RBAC-lite 基础。

---

# 七、和普通 Agent / RAG 的区别

| 对比点 | 普通 RAG / Agent | ClinMindRuntime |
|---|---|---|
| 主控 | LLM / Agent | Java Runtime Core |
| 状态 | 多数弱状态 | RuntimeState |
| 安全 | Prompt 约束为主 | SafetyGate + DecisionBoundary |
| 知识 | 检索后拼 Prompt | EvidenceRef → EvidenceGraph |
| 输出 | 通常单一回答 | PatientOutput / ClinicianReport 分角色 |
| 追踪 | 日志为主 | RuntimeTrace + asset version |
| 能力证明 | Demo 效果 | EvaluationCaseSet + Scorer + EvaluationResult |
| 经验沉淀 | 常常自动记忆 | Candidate + Review Required |
| 治理 | 弱治理 | CandidateSanitizer + SourceRef + Review + AuditLog |
| 持久化 | 可选日志 | PostgreSQL-backed governance runtime |
| Console | 后补管理页 | RBAC-lite + Safe DTO + Audit Center |

---

# 八、代码中对应的软件工程模式

```text
RuntimeService：Orchestrator / Facade
Provider Interface：Ports and Adapters
EvaluationScorer：Strategy
YamlEvaluationCaseRepository：Repository
CapabilityProfileProposalService：Policy / Governance
RuntimeTrace：Trace-driven observability
AssetVersion：Version Binding
Patient / Clinician DTO：Output Boundary / Mapper
CandidateGenerationPolicy：Policy
CandidateStore：Repository
CandidateSanitizer：Policy / Sanitizer
CandidateSourceRefFactory：Factory
CandidateReviewService：Application Service
AuditLogService：Governance Service
JdbcStore：Adapter / Repository Implementation
AccessPolicy：Policy
SafeConsoleDtoMapper：Output Boundary / Mapper
ConsoleQueryService：Application Service
```

详细解释见：

```text
docs/架构模式与设计模式说明.md
```

---

# 九、下一步建议

Phase 5-P1 设计已经完成。下一步不是继续补总体规划，而是：

```text
1. 读取 docs/Phase5_P1开发任务清单.md。
2. 将 Phase5-P1-A 从 [ ] 改为 [/]。
3. 实现 ActorContext / DebugRole / AccessPolicy / RolePolicy。
4. 补基础单元测试。
5. 不实现全部 Console API，不做完整前端、RAG、模型训练或正式 RBAC。
```

---

# 十、项目边界声明

当前项目是：

```text
医疗 AI Runtime Core + Asset Governance + Evaluation Loop + Candidate Governance + Persistence Governance + Minimal Console Governance 的工程原型。
```

当前项目不是完整产品化系统，也不替代真实医疗流程。

它展示的是：

```text
AI 能力如何在高风险场景中被控制、评估、追踪、复盘、审核、持久化治理，并逐步进入可访问治理边界。
```
