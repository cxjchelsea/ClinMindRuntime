export function RuntimePage() {
  return (
    <section className="page-placeholder">
      <h1>Runtime Sessions</h1>
      <p>查看 Runtime 会话摘要与 Safe DTO 详情（不含患者原文）。</p>
      <p className="page-placeholder__hint">页面数据对接将在后续 P2 子阶段实现。</p>
    </section>
  );
}
