package com.clinmind.runtime.evidence.graph;

public enum GraphEvidenceStatus {
    SUCCESS,
    NO_GRAPH_PATH_FOUND,
    POLICY_REJECTED,
    VALIDATION_REJECTED,
    DEGRADED,
    FAILED,
    SKIPPED
}
