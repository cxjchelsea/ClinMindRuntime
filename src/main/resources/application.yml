spring:
  application:
    name: clinmind-runtime
  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    com.clinmind.runtime: INFO

clinmind:
  persistence:
    mode: in-memory
  debug-api:
    enabled: true
    require-debug-token: false
    debug-token: ""
