spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinmind
    username: clinmind
    password: clinmind
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

clinmind:
  persistence:
    mode: postgres
